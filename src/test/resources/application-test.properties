spring.data.mongodb.auto-index-creation=false
# Same Atlas cluster as production (MainDatabase) but a SEPARATE database — never point this at "mchub"
spring.data.mongodb.uri=${MONGODB_TEST_URI}
spring.data.mongodb.database=${MONGODB_TEST_DATABASE:mchub_test}

# Disable dotenv loading in tests
spring.config.import=optional:file:.env[.properties]

# JWT secret for tests
mchub.jwt.secret=test-secret-key-for-unit-tests-very-long-value
mchub.jwt.expiration=86400000

# Disable security auto-config clashing in WebMvcTest
spring.security.user.name=test
spring.security.user.password=test
