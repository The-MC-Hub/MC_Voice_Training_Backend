package com.mchub.enums;

public enum LearningPathType {
    /** Standard course: fixed bundle of lessons + readings + quiz → certificate */
    STRUCTURED_COURSE,

    /** Milestone-based progression path (future use) */
    MILESTONE_PATH,

    /** No fixed content structure; user picks their own practice */
    SELF_PACED
}
